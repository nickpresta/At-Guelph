*At Guelph*

To run:
  From this directory: python -m SimpleHTTPServer
  Open your browser to http://localhost:8000/index.html
  (this needs to run from a server, not local files)

Test with nginx as well (default config with root set to this directory)
